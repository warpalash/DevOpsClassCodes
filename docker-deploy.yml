- hosts: dev
  become: true
  tasks:
    - name: Install docker
      apt:
        name: docker
        state: present
        update_cache: 'yes'
    - name: start docker 
      apt:
        name: docker
        state: started
    - name: Start docker container
      docker_container:
        name: addbook
        image: 'babban27/addbook:{{dockertag}}'
        state: started
        published_ports:
          - '0.0.0.0:8080:8080'

